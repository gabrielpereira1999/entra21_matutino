package acao;

import javax.swing.table.DefaultTableModel;

import dados.Dado;

public class Acao {
	
	public static String Vale, Imposto, SalLiquido;

	// Incrementa qntd
	public void incrementa(String c) {

		//Verifica produto
		if (c.equals("Carro Vermelho")) {
			
		

		//Dado.arrayColaboradores.add(c);

	}

	// Retorna TableModel
	public DefaultTableModel atualiza() {
		DefaultTableModel modelo = new DefaultTableModel();
		modelo.addColumn("Colaborador");
		modelo.addColumn("Salário Líquido");

		for (int i = 0; i < Dado.arrayColaboradores.size(); i++) {
			modelo.addRow(new Object[] { Dado.arrayColaboradores.get(i).getNome(),
					Dado.arrayColaboradores.get(i).getSalLiquido(), });
		}
		return modelo;
	}
	
	/*public void cargos(Colaborador c) {
		boolean val = false;
		for(int i =0; i<Dado.arrayCargos.size();i++) {
			if( c.getCargo().equals(Dado.arrayCargos.get(i)[0]) ) {
				Dado.arrayCargos.set(i, new String[] {c.getCargo(),""+(Integer.parseInt(Dado.arrayCargos.get(i)[1])+1)});
				val = true;
				break;
			}
		}
		if(!val) {
			Dado.arrayCargos
		}
	}*/
	
	

}
